#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-wp-i18n/bin/wpi18n" "$@"
  ret=$?
else 
  node  "$basedir/../node-wp-i18n/bin/wpi18n" "$@"
  ret=$?
fi
exit $ret
